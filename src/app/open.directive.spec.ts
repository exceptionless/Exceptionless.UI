import { OpenDirective } from './open.directive';

describe('OpenDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDirective();
    expect(directive).toBeTruthy();
  });
});
